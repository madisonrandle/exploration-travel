
const chai = require("chai");
const expect = chai.expect;
import Agent from '../classes/agent';

let agent;

describe.only('Agent', () => {
   beforeEach(() => {
     agent = new Agent();
   });


})
//
